"""Local config overrides — copy to config_local.py and fill in your values."""
UE_SOURCE = "/path/to/UnrealEngine"       # UE 源码根目录
GIT_MAIN_REPO = "/path/to/UnrealEngine"   # Git 主仓库
LLM_BASE_URL = "https://api.openai.com/v1"
LLM_MODEL = "gpt-4o"
# LLM_API_KEY is read from env var LLM_API_KEY — set it in your shell or .env
